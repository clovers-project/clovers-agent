[project]
name = "clovers-agent-market-analysis"
version = "0.1.2"
description = ""
readme = "README.md"
requires-python = ">=3.12, <4.0.0"
dependencies = ["clovers-agent>=0.2.10", "akshare", "pandas>=3.0.5"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.uv.sources]
clovers-agent = { workspace = true }
