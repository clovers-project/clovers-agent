[project]
name = "clovers-agent-workspace"
version = "0.1.0"
description = "CloversAgent Workspace"
readme = "README.md"
requires-python = ">=3.12,<4.0.0"

[tool.uv]
workspace = { members = ["clovers-agent", "clovers_agent_toolkit"] }
