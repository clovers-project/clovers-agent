[project]
name = "clovers-agent-embedding"
version = "0.2.3"
description = ""
authors = [{ name = "KarisAya", email = "karisaya@foxmail.com" }]
readme = "README.md"
requires-python = ">=3.12,<4.0.0"
dependencies = ["numpy>=2.4.4", "torch>=2.5.0", "sentence-transformers>=5.3.0"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["clovers_agent"]

[dependency-groups]
dev = ["jupyter>=1.1.1", "jupyterlab-language-pack-zh-cn>=4.5.post2"]
